import React, { Component } from 'react';


const  UserSubmit = (props) => {
    return (
        <div>
            <button onClick={props.checkInput}>submit</button>
        </div>
    )
}

export default UserSubmit